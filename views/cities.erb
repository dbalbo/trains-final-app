<h3> Here are all the cities in the system</h3>
 <% if @cities.any?() %>
   <p>cities</p>
     <ul>
       <% @cities.each do |city| %>
         <li><a href="/cities/<%= city.id() %>"><%= city.name() %></a></li>

       <% end %>
      </ul>

      <% else %>
      <p>There are no cities added yet.</p>
      <% end %>

      <p>Add a city to the database</p>


        <form class="form-inline" action='/cities' method='post'>
          <label for='name'></label>
          <input id='name' name='name' class="form-control" type='text'>    

        <button type='submit' class="btn btn-primary">Add City</button>
      </form>

      <div class ="container">
        <a href="/">Return Home</a><br>
        <a href="/trains">View All Train Schedules</a>
      </div>
